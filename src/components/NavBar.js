import MenuIcon from "../assets/MenuIcon";

const NavBar = () => {
    return (
        <header id="nav-bar">
            <div className="wrapper">
                <h1>Portfolio</h1>
                <MenuIcon />
            </div>
        </header>
    );
};

export default NavBar;
