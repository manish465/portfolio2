import MenuIcon from "../assets/MenuIcon";

const NavBar = () => {
    return (
        <header id="nav-bar">
            <h1>Portfolio</h1>
            <MenuIcon />
        </header>
    );
};

export default NavBar;
