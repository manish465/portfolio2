const ThemeToggle = () => {
    return (
        <div class="toggle-btn" id="toggle-btn">
            <input type="checkbox" defaultChecked={true} />
            <span></span>
        </div>
    );
};

export default ThemeToggle;
